name: cmake build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: true
    - name: run-cmake
      uses: lukka/run-cmake@v10.1
